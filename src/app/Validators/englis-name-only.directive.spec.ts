import { EnglisNameOnlyDirective } from './englis-name-only.directive';

describe('EnglisNameOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EnglisNameOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
